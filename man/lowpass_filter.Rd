% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{lowpass_filter}
\alias{lowpass_filter}
\title{Low-pass spatial filter}
\usage{
lowpass_filter(x, w = 3)
}
\arguments{
\item{x}{A \link[terra:SpatRaster]{terra::SpatRaster}.}

\item{w}{Neighbourhood size passed to \code{\link[terra:focal]{terra::focal()}}. A single odd
integer gives a \verb{w x w} window (default \code{3}, i.e. a 3x3 mean).}
}
\value{
A \link[terra:SpatRaster]{terra::SpatRaster} with the same layers as \code{x}, smoothed.
}
\description{
Applies a low-pass (moving-average) spatial filter to every layer of a
raster, replacing each cell with the mean of its neighbourhood. This
smooths the speckle caused by residual glint and water-surface roughness
before computing the ratio transform. It is a thin, documented wrapper
around \code{\link[terra:focal]{terra::focal()}}.
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
smoothed <- lowpass_filter(img[["blue"]], w = 3)

}
\seealso{
\code{\link[=correct_glint]{correct_glint()}}, \code{\link[=log_ratio]{log_ratio()}}
}
