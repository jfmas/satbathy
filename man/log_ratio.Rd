% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_ratio.R
\name{log_ratio}
\alias{log_ratio}
\title{Log-ratio transform (Stumpf et al. 2003)}
\usage{
log_ratio(x, blue = "blue", green = "green", n = 10000, trim = c(0.01, 0.99))
}
\arguments{
\item{x}{A multi-band \link[terra:SpatRaster]{terra::SpatRaster} of surface reflectance. Non-water
areas should be masked beforehand (see \code{\link[=water_mask]{water_mask()}}); cells with
non-positive reflectance yield \code{NA}.}

\item{blue, green}{Band identifiers (layer names or integer indices) for the
high-penetration (blue) and lower-penetration (green) bands.}

\item{n}{Fixed positive constant applied inside the logarithms, chosen so
both logarithms stay positive. Defaults to \code{10000}. Stumpf et al. (2003)
report the ratio to be insensitive to \code{n} (root-mean-square error changing
by less than 0.4 m for \code{n} between 500 and 1500), as it only rescales the
ratio, which is absorbed by the calibration coefficients.}

\item{trim}{Length-2 numeric vector of lower/upper probabilities used to
trim outliers from the ratio: cells outside these quantiles are set to
\code{NA}. Defaults to \code{c(0.01, 0.99)}. Use \code{NULL} to disable trimming.}
}
\value{
A single-layer \link[terra:SpatRaster]{terra::SpatRaster} named \code{"ratio"}.
}
\description{
Computes the log-ratio transform used to linearise the relationship
between satellite reflectance and water depth:
\deqn{R = \frac{\ln(n \cdot R_{blue})}{\ln(n \cdot R_{green})}}
where \eqn{R_{blue}} and \eqn{R_{green}} are the reflectance of a
high-penetration and a lower-penetration band (typically blue and green),
and \eqn{n} is a fixed constant chosen so that both logarithms stay
positive. Because the more strongly attenuated band changes faster with
depth, the ratio increases monotonically with depth over its valid range.
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
wm  <- water_mask(img, green = "green", nir = "nir")
img <- terra::mask(img, wm, maskvalues = c(FALSE, NA))
ratio <- log_ratio(img, blue = "blue", green = "green")
ratio

}
\references{
Stumpf RP, Holderied K, Sinclair M (2003). Determination of water depth
with high-resolution satellite imagery over variable bottom types.
Limnology and Oceanography 48(1, part 2):547-556.
\doi{10.4319/lo.2003.48.1_part_2.0547}
}
\seealso{
\code{\link[=fit_bathymetry]{fit_bathymetry()}}, \code{\link[=correct_glint]{correct_glint()}}, \code{\link[=lowpass_filter]{lowpass_filter()}}
}
