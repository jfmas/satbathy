% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_bathymetry}
\alias{fit_bathymetry}
\title{Fit an empirical bathymetry model}
\usage{
fit_bathymetry(data, ratio = "ratio", depth = "depth", max_depth = NULL)
}
\arguments{
\item{data}{A \code{data.frame} with the ratio and depth columns, typically the
output of \code{\link[=extract_ratio]{extract_ratio()}}.}

\item{ratio, depth}{Column names in \code{data} for the ratio and the measured
depth. Default to \code{"ratio"} and \code{"depth"}.}

\item{max_depth}{Optional maximum depth (a negative number). Only points
with \code{depth > max_depth} (and \code{depth < 0}) are used for calibration. If
\code{NULL} (default) all rows with \code{depth < 0} are used.}
}
\value{
An object of class \code{satbathy_model}: a list with the fitted \code{lm}
(\code{model}), the \code{data} actually used, the \code{max_depth} threshold, the
adjusted R-squared (\code{r.squared}), the number of points (\code{n}) and the
\code{call}.
}
\description{
Fits the empirical linear model relating depth to the log-ratio,
\eqn{Z = m_1 R - m_0} (Stumpf et al. 2003), by ordinary least squares.
Optionally restricts the calibration to points shallower than a maximum
depth, since the ratio saturates and departs from linearity beyond the
penetration limit (see \code{\link[=optimal_max_depth]{optimal_max_depth()}}).
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
pts <- terra::vect(system.file("extdata", "example_points.gpkg",
                               package = "satbathy"))
img <- terra::mask(img, water_mask(img), maskvalues = c(FALSE, NA))
tab <- extract_ratio(log_ratio(img), pts)
fit <- fit_bathymetry(tab, max_depth = -16)
fit

}
\references{
Stumpf RP, Holderied K, Sinclair M (2003). Determination of water depth
with high-resolution satellite imagery over variable bottom types.
Limnology and Oceanography 48(1, part 2):547-556.
\doi{10.4319/lo.2003.48.1_part_2.0547}
}
\seealso{
\code{\link[=extract_ratio]{extract_ratio()}}, \code{\link[=predict_bathymetry]{predict_bathymetry()}}, \code{\link[=optimal_max_depth]{optimal_max_depth()}}
}
