% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/max_depth.R
\name{optimal_max_depth}
\alias{optimal_max_depth}
\title{Estimate the maximum reliable depth}
\usage{
optimal_max_depth(
  data,
  depths = NULL,
  min_n = 10,
  ratio = "ratio",
  depth = "depth"
)
}
\arguments{
\item{data}{A \code{data.frame} with ratio and depth columns, typically from
\code{\link[=extract_ratio]{extract_ratio()}}.}

\item{depths}{Optional numeric vector of maximum-depth thresholds (negative
values). If \code{NULL} (default) a sequence from \code{-10} down to the deepest
observed point, in 1 m steps, is used.}

\item{min_n}{Minimum number of points required to fit a model at a given
threshold. Thresholds with fewer points yield \code{NA} metrics. Defaults to
\code{10}.}

\item{ratio, depth}{Column names in \code{data}. Default to \code{"ratio"}, \code{"depth"}.}
}
\value{
A \code{data.frame} with one row per threshold and columns
\code{max_depth}, \code{n} (points used), \code{r2} (adjusted R-squared) and \code{rmse}
(in-sample root-mean-square error).
}
\description{
Determines how deep the log-ratio keeps a linear relationship with depth,
i.e. the maximum depth the satellite signal can resolve before it
saturates. The calibration is repeated for a decreasing sequence of
maximum-depth thresholds; for each threshold a linear model is fitted with
the points shallower than it and its goodness of fit is recorded. The
adjusted R-squared typically increases as more of the linear range is
included and then drops once saturated (too-deep) points are added,
revealing the usable depth limit.
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
pts <- terra::vect(system.file("extdata", "example_points.gpkg",
                               package = "satbathy"))
img <- terra::mask(img, water_mask(img), maskvalues = c(FALSE, NA))
tab <- extract_ratio(log_ratio(img), pts)
md <- optimal_max_depth(tab)
md[which.max(md$r2), ]

}
\seealso{
\code{\link[=fit_bathymetry]{fit_bathymetry()}}
}
