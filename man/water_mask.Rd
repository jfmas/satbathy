% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/water_mask.R
\name{water_mask}
\alias{water_mask}
\title{Water mask from NDWI}
\usage{
water_mask(x, green = "green", nir = "nir", threshold = 0)
}
\arguments{
\item{x}{A multi-band \link[terra:SpatRaster]{terra::SpatRaster} of surface reflectance.}

\item{green, nir}{Band identifiers (layer names or integer indices) for the
green and near-infrared bands of \code{x}.}

\item{threshold}{Numeric NDWI threshold above which a pixel is considered
water. Defaults to \code{0}.}
}
\value{
A single-layer logical \link[terra:SpatRaster]{terra::SpatRaster} named \code{"water"}
(\code{TRUE} for water, \code{FALSE} otherwise).
}
\description{
Builds a logical water mask by thresholding the NDWI (see \code{\link[=ndwi]{ndwi()}}).
Pixels with \code{NDWI > threshold} are flagged as water (\code{TRUE}).
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
wm <- water_mask(img, green = "green", nir = "nir", threshold = 0)
# keep only water pixels of the image:
water_only <- terra::mask(img, wm, maskvalues = c(FALSE, NA))

}
\seealso{
\code{\link[=ndwi]{ndwi()}}
}
