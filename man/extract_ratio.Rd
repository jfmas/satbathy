% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{extract_ratio}
\alias{extract_ratio}
\title{Extract ratio values at in situ depth points}
\usage{
extract_ratio(ratio, points, depth_field = "depth")
}
\arguments{
\item{ratio}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} of ratio values.}

\item{points}{In situ bathymetric points, either a \link[terra:SpatVector]{terra::SpatVector} or
an \code{sf} object of points. Reprojected to the CRS of \code{ratio} if needed.}

\item{depth_field}{Name of the attribute in \code{points} holding the measured
depth (negative below the surface). Defaults to \code{"depth"}.}
}
\value{
A \code{data.frame} with columns \code{ratio} and \code{depth}, with rows
containing missing values removed.
}
\description{
Builds the modelling table used to calibrate the bathymetry model by
extracting the log-ratio value (see \code{\link[=log_ratio]{log_ratio()}}) at each in situ
bathymetric point and pairing it with the measured depth.
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
pts <- terra::vect(system.file("extdata", "example_points.gpkg",
                               package = "satbathy"))
wm  <- water_mask(img)
img <- terra::mask(img, wm, maskvalues = c(FALSE, NA))
ratio <- log_ratio(img)
tab <- extract_ratio(ratio, pts, depth_field = "depth")
head(tab)

}
\seealso{
\code{\link[=fit_bathymetry]{fit_bathymetry()}}
}
