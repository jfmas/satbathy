% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{predict_bathymetry}
\alias{predict_bathymetry}
\title{Predict depth from a ratio raster or table}
\usage{
predict_bathymetry(model, ratio)
}
\arguments{
\item{model}{A \code{satbathy_model} from \code{\link[=fit_bathymetry]{fit_bathymetry()}}.}

\item{ratio}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} of ratio values (its layer
is treated as the \code{ratio} predictor).}
}
\value{
A single-layer \link[terra:SpatRaster]{terra::SpatRaster} named \code{"depth"}.
}
\description{
Applies a fitted \code{\link[=fit_bathymetry]{fit_bathymetry()}} model to new ratio values to obtain
predicted depths. When \code{newdata} is a raster the result is a depth map.
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
pts <- terra::vect(system.file("extdata", "example_points.gpkg",
                               package = "satbathy"))
img <- terra::mask(img, water_mask(img), maskvalues = c(FALSE, NA))
r   <- log_ratio(img)
fit <- fit_bathymetry(extract_ratio(r, pts), max_depth = -16)
depth_map <- predict_bathymetry(fit, r)

}
\seealso{
\code{\link[=fit_bathymetry]{fit_bathymetry()}}
}
