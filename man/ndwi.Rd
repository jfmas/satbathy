% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/water_mask.R
\name{ndwi}
\alias{ndwi}
\title{Normalized Difference Water Index (NDWI)}
\usage{
ndwi(x, green = "green", nir = "nir")
}
\arguments{
\item{x}{A multi-band \link[terra:SpatRaster]{terra::SpatRaster} of surface reflectance.}

\item{green, nir}{Band identifiers (layer names or integer indices) for the
green and near-infrared bands of \code{x}.}
}
\value{
A single-layer \link[terra:SpatRaster]{terra::SpatRaster} named \code{"ndwi"}.
}
\description{
Computes McFeeters' Normalized Difference Water Index,
\code{(green - nir) / (green + nir)}. Water typically yields positive values
while land and clouds yield negative values, so the NDWI is a convenient
basis for building a water mask when a sensor quality band is not
available (e.g. PlanetScope imagery).
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
ndwi(img, green = "green", nir = "nir")

}
\references{
McFeeters SK (1996). The use of the Normalized Difference Water Index
(NDWI) in the delineation of open water features. International Journal of
Remote Sensing 17(7):1425-1432.
}
\seealso{
\code{\link[=water_mask]{water_mask()}}
}
