% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glint.R
\name{correct_glint}
\alias{correct_glint}
\title{Sun-glint correction (Hedley et al. 2005)}
\usage{
correct_glint(
  x,
  vis_bands = c("blue", "green"),
  nir_band = "nir",
  deep_mask,
  n_sample = 500,
  prob = 0.01
)
}
\arguments{
\item{x}{A multi-band \link[terra:SpatRaster]{terra::SpatRaster} of surface reflectance.}

\item{vis_bands}{Band identifiers (layer names or integer indices) of the
visible bands to correct, e.g. \code{c("blue", "green")}.}

\item{nir_band}{Band identifier of the near-infrared band.}

\item{deep_mask}{A \link[terra:SpatVector]{terra::SpatVector} (or SpatRaster) delimiting optically
deep water, used to sample glint and estimate \code{min_NIR}. Reprojected to
the CRS of \code{x} if necessary.}

\item{n_sample}{Number of random deep-water pixels used to fit each
regression. Defaults to \code{500}.}

\item{prob}{Lower quantile of NIR over deep water used to estimate the
glint-free NIR reflectance (\code{min_NIR}). Defaults to \code{0.01}.}
}
\value{
A copy of \code{x} in which the layers named in \code{vis_bands} have been
glint-corrected. If the deep-water sample is too small to fit the
regressions, \code{x} is returned unchanged with a warning.
}
\description{
Removes sun-glint from visible bands using the image-based method of
Hedley et al. (2005). For each visible band, a linear regression against
the near-infrared (NIR) band is fitted over a deep-water sample, where the
bottom contribution is assumed negligible and NIR reflectance is due to
glint alone. The band is then corrected as
\code{band - slope * (NIR - min_NIR)}, where \code{min_NIR} is the glint-free NIR
reflectance estimated as a low quantile of NIR over deep water.
}
\examples{
img  <- terra::rast(system.file("extdata", "example_sr.tif",
                                package = "satbathy"))
deep <- terra::vect(system.file("extdata", "example_deepmask.gpkg",
                                package = "satbathy"))
corrected <- correct_glint(img, vis_bands = c("blue", "green"),
                           nir_band = "nir", deep_mask = deep)

}
\references{
Hedley JD, Harborne AR, Mumby PJ (2005). Technical note: Simple and robust
removal of sun glint for mapping shallow-water benthos. International
Journal of Remote Sensing 26(10):2107-2112.
\doi{10.1080/01431160500034086}
}
\seealso{
\code{\link[=log_ratio]{log_ratio()}}, \code{\link[=lowpass_filter]{lowpass_filter()}}
}
