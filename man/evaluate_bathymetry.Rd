% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{evaluate_bathymetry}
\alias{evaluate_bathymetry}
\title{Evaluate a bathymetry model against independent data}
\usage{
evaluate_bathymetry(model, newdata, ratio = "ratio", depth = "depth")
}
\arguments{
\item{model}{A \code{satbathy_model} from \code{\link[=fit_bathymetry]{fit_bathymetry()}}.}

\item{newdata}{A \code{data.frame} with ratio and observed depth columns
(e.g. from \code{\link[=extract_ratio]{extract_ratio()}} on held-out points).}

\item{ratio, depth}{Column names for the ratio and observed depth in
\code{newdata}. Default to \code{"ratio"} and \code{"depth"}.}
}
\value{
A one-row \code{data.frame} with columns \code{n}, \code{rmse} and \code{mae}.
}
\description{
Predicts depth for a test table and returns accuracy metrics.
}
\examples{
img <- terra::rast(system.file("extdata", "example_sr.tif",
                               package = "satbathy"))
pts <- terra::vect(system.file("extdata", "example_points.gpkg",
                               package = "satbathy"))
img <- terra::mask(img, water_mask(img), maskvalues = c(FALSE, NA))
tab <- extract_ratio(log_ratio(img), pts)
fit <- fit_bathymetry(tab, max_depth = -16)
evaluate_bathymetry(fit, tab)

}
\seealso{
\code{\link[=rmse]{rmse()}}, \code{\link[=mae]{mae()}}
}
