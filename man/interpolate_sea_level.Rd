% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sea_level.R
\name{interpolate_sea_level}
\alias{interpolate_sea_level}
\title{Interpolate tide-gauge sea level at arbitrary times}
\usage{
interpolate_sea_level(
  target_time,
  df,
  time_col = "datetime",
  level_col = "nivel"
)
}
\arguments{
\item{target_time}{Time(s) at which to interpolate the sea level. Numeric,
\code{Date}, or \code{POSIXct}; a vector is processed in one call.}

\item{df}{A \code{data.frame} holding the tide-gauge record.}

\item{time_col, level_col}{Names of the columns in \code{df} with the measurement
time and the sea level. Default to \code{"datetime"} and \code{"nivel"}.}
}
\value{
A numeric vector, the same length as \code{target_time}, with the
interpolated sea level (\code{NA} outside the record's time range).
}
\description{
Linearly interpolates a tide-gauge sea-level record at one or more target
times. This is used to normalise in situ echo-sounder depths to a common
datum: each sounding is referred to the sea level measured by the tide
gauge at the moment it was taken, removing the effect of the tide between
and during survey dates.
}
\details{
The function is a thin, vectorised wrapper around \code{\link[stats:approx]{stats::approx()}} with
\code{rule = 1}, so target times outside the range of the record return \code{NA}.
}
\examples{
gauge <- data.frame(
  datetime = as.POSIXct("2025-04-28 13:00", tz = "UTC") +
    c(0, 600, 1200, 1800),
  nivel = c(5.57, 5.58, 5.60, 5.59)
)
# sea level at two sounding times
times <- as.POSIXct(c("2025-04-28 13:05", "2025-04-28 13:25"), tz = "UTC")
interpolate_sea_level(times, gauge)

}
\seealso{
\code{\link[=extract_ratio]{extract_ratio()}}, \code{\link[=fit_bathymetry]{fit_bathymetry()}}
}
